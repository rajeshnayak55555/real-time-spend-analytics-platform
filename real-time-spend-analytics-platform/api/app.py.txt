from fastapi import FastAPI
import joblib

app = FastAPI()

model = joblib.load("model.pkl")

@app.get("/")
def home():
    return {"message": "Spend Analytics API Running"}

@app.get("/predict")
def predict(amount: float):
    result = model.predict([[amount]])
    return {"fraud_prediction": int(result[0])}